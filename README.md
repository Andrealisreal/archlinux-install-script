# 🐧 Automated Arch Linux Installation Script

> ⚠️ **Внимание:** этот скрипт полностью стирает выбранный диск!  
> Убедитесь, что вы указали **правильное устройство** перед запуском.  
> Автор не несёт ответственности за потерю данных — используйте **на свой страх и риск**.

---

## 📖 Описание

Скрипт предназначен для **автоматизированной установки Arch Linux** с минимальным вмешательством пользователя.  
Вы можете отредактировать файл и изменить:
- список устанавливаемых пакетов;
- разметку диска;
- имя пользователя;
- параметры загрузчика и другие настройки.

Скрипт создавался **исключительно в личных целях**, но опубликован на GitHub для удобства использования и сохранения.

---

## ⚙️ Возможности

- Полная автоматическая установка Arch Linux (Gnome, NVIDIA, Bluetooth и т.д.)
- Настройка часового пояса, локали и hostname  
- Создание пользователя и установка паролей  
- Автоматическое включение NetworkManager, GDM и Bluetooth  
- Настройка **zram** в качестве swap  
- Установка шрифтов, утилит и популярных приложений (Chromium, Telegram, Discord)  
- Поддержка **UEFI (EFI)** загрузки с использованием **GRUB**  
- Генерация `fstab` и готовность системы к первому запуску  

---

## 🧰 Требования

1. **Загруженный Arch Linux LiveCD** (установочный образ).
2. Рабочее подключение к интернету:
   - Кабельное (Ethernet) — готово к использованию.
   - Wi-Fi — настройка описана ниже.
3. **Скрипт установки**, размещённый на флешке.

---

## 🌐 Подключение к Wi-Fi (если требуется)

Если вы используете Wi-Fi, выполните следующие шаги:

```bash
iwctl
```

Внутри `iwctl`:

```bash
device list            # Проверяем доступные устройства
station list           # Обычно wlan0
station wlan0 scan
station wlan0 get-networks
station wlan0 connect "Имя_сети"
quit
```

Если Wi-Fi не работает:

```bash
rfkill list            # Проверяем блокировку
rfkill unblock all     # Снимаем блокировку
```

---

## 💽 Подготовка и запуск установки

1. Посмотреть подключённые диски:
   ```bash
   lsblk
   ```

2. Смонтировать флешку:
   ```bash
   mkdir /mnt/usb
   mount /dev/sda1 /mnt/usb   # замените sda1 на свой вариант
   ```

3. Проверить содержимое флешки:
   ```bash
   ls /mnt/usb
   ```

4. Сделать скрипт исполняемым:
   ```bash
   chmod +x /mnt/usb/arch_install.sh
   ```

5. Запустить установку:
   ```bash
   /mnt/usb/arch_install.sh
   ```

После этого **всё будет выполняться автоматически**. Скрипт сопровождает каждый шаг комментариями и цветными подсказками.

---

## 🧹 Очистка системы после установки

После перезагрузки можно запустить дополнительный скрипт для очистки системы:

👉 [clean-system.sh](https://github.com/Andrealisreal/archlinux-install-script/blob/main/clean-system.sh)

Он выполняет:
- очистку кэша `pacman`;
- удаление временных файлов;
- поиск и удаление осиротевших пакетов;
- оптимизацию базы пакетов;
- дефрагментацию Btrfs (если используется).

> 💡 Перед запуском убедитесь, что не изменяли список пакетов — при необходимости отредактируйте скрипт под себя.

---

## ⚠️ Важные предупреждения

- Скрипт **удаляет все данные** с выбранного диска.  
- Перед запуском проверьте корректность устройства (например: `/dev/nvme0n1`).  
- Используйте только в режиме **UEFI**.  
- Проверьте наличие интернет-соединения.  
- Скрипт не предназначен для dual-boot без модификации.

---

## 🧑‍💻 Автор

**Andreal** — энтузиаст Arch Linux, автоматизации и минимализма.  
Скрипт создан ради удобства, изучения и удовольствия от процесса 🧠💻

---

## 📜 Лицензия

Этот проект распространяется **"как есть"**, без каких-либо гарантий.  
Вы можете свободно использовать, изменять и распространять его при условии упоминания автора.

---
